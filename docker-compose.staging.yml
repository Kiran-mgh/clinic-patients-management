version: '3.8'

services:
  staging-backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: staging-amar-backend
    ports:
      - "3001:3000"
    env_file:
      - .env
    restart: always
